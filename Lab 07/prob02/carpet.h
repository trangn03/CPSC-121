#include "cpputils/graphics/image.h"

// Draws a Sierpinski Carpet on the given image the given order and size,
// with the top left corner at (x, y).
// You do NOT need to edit this function prototype.
void DrawCarpet(int x, int y, int size, int order, graphics::Image& image);
